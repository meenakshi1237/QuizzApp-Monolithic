spring:
  application:
    name: quizzapp

  datasource:
    url: jdbc:postgresql://localhost:5432/questiondb

    username: postgres
    password: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

